# Inverse of `vec_symm`

Inverse of `vec_symm`

## Usage

``` r
unvec_symm(x, dim)
```

## Arguments

- x:

  A numeric vector of length `n*(n+1)/2`.

- dim:

  The matrix dimension `n`.

## Value

The corresponding `n x n` symmetric matrix.
